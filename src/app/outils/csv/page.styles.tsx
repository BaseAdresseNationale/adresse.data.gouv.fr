'use client'

import styled from 'styled-components'

export const TextWrapper = styled.div`

    .filters {
      margin: 1em 0;
    }

    .loading {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 1em 0;
    }

    .error {
      color: red;
    }
  `
