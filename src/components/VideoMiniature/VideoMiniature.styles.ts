'use client'

import styled from 'styled-components'

export const StyledWrapper = styled.a`
    margin: 10px;
    h3 {
        font-size: 0.9rem;
        font-weight: bold;
        margin: 0;
    }

    &::after {
        display: none;
    }
`
